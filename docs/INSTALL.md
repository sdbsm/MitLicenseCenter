# Установка MitLicense Center

Документ описывает установку на хост заказчика: системные требования, пошаговое
прохождение мастера, учётные записи и права, обновление поверх существующей версии,
удаление.

---

## 1. Системные требования

### Операционная система и роли

| Компонент | Требование |
|---|---|
| ОС | Windows Server 2019 / 2022, 64-разрядная (x64) |
| IIS | Роль веб-сервера IIS установлена и работает |
| SQL Server | SQL Server 2019 / 2022 (любой редакции; Express достаточен для малых нагрузок) |
| Платформа 1С | Установлена платформа 1С:Предприятие с утилитами `rac.exe`, `ras.exe`, `webinst.exe` |
| Права установщика | Локальный администратор (требует `PrivilegesRequired=admin` в `.iss`) |

### .NET runtime

Артефакт собирается **self-contained single-file win-x64** (`--self-contained true
-p:PublishSingleFile=true` в `publish-release.ps1`): среда выполнения .NET вшита в
`MitLicenseCenter.Web.exe`. Устанавливать .NET на хост **не требуется**.

### Учётная запись службы и предусловие по SQL (ADR-49)

Служба работает под Windows-учётной записью и подключается к **локальному** SQL Server по
доверенному подключению Windows (`Trusted_Connection`) — **SQL-логин и пароль в конфигурации
не используются.** В мастере выбирается одна из учётных записей службы:

- **Виртуальная учётная запись `NT SERVICE\MitLicenseCenter` (по умолчанию, рекомендуется)** —
  без пароля, подходит и для рабочей группы, и для домена. Ввод учётных данных и тест
  подключения не требуются.
- **Именованная Windows-учётка / gMSA (доменный сценарий)** — `ДОМЕН\Пользователь` (обычная
  учётка с паролем) или `ДОМЕН\Имя$` (gMSA, без пароля — отмечается чекбоксом в мастере).

**SQL-логин учётки службы создаёт сам установщик** (`CREATE LOGIN … FROM WINDOWS` + роль
`sysadmin`, идемпотентно; **всегда Windows-логин**) — заранее его готовить не нужно (для
виртуальной учётки это и невозможно: её SID появляется только при регистрации службы).
Назначить `sysadmin` может только тот, кто сам `sysadmin`, поэтому провижининг идёт под
`sysadmin`-личностью **на выбор** (страница мастера «Подключение установщика к SQL», §3.4):
Integrated Security запускающего администратора (по умолчанию) либо введённый SQL-логин с
ролью `sysadmin`. Отсюда **предусловие установки:**

> Установщику должна быть доступна **какая-либо `sysadmin`-личность на локальном экземпляре
> SQL Server** — либо запускающий администратор сам `sysadmin` (Integrated Security, по
> умолчанию; работает и на «только Windows-аутентификация»), либо вводится SQL-логин с ролью
> `sysadmin` (требует экземпляра в смешанном режиме). Иначе авто-создание SQL-логина учётки
> службы невозможно и установщик прерывается с откатом.

---

## 2. Что устанавливается

### Служба Windows

| Параметр | Значение |
|---|---|
| Имя службы (`sc create`) | `MitLicenseCenter` |
| Отображаемое имя | `MitLicense Center` |
| Тип запуска | `auto` (автоматический) |
| Исполняемый файл | `{app}\MitLicenseCenter.Web.exe` |
| Kestrel, порт | `http://+:<порт>` — по умолчанию `8080`, задаётся в мастере |
| Учётная запись службы | `NT SERVICE\MitLicenseCenter` (по умолчанию) либо именованная Windows-учётка / gMSA |
| Подключение к SQL | доверенное подключение Windows (`Trusted_Connection=True`) |

Каталог установки по умолчанию: `%ProgramFiles%\MitLicense Center`
(`DefaultDirName={autopf}\MitLicense Center` в `.iss`).

### Каталог данных

`%ProgramData%\MitLicenseCenter` — создаётся секцией `[Dirs]` с флагом
`uninsneveruninstall` (не удаляется при деинсталляции автоматически).

Содержимое:

- **key ring** — файлы ASP.NET Data Protection (назначение `mlc.settings.v1`);
  ключи расшифровывают секретные настройки в `dbo.Settings` (см. ADR-8).
- **`initial-admin.secret`** — одноразовый файл с паролем первого администратора
  (только чистая установка; удаляется бэкендом при первом старте, см. §5).

### Конфигурационный файл

`{app}\appsettings.Production.json` — генерируется мастером из ввода оператора.
Содержит строки подключения (Connection Strings) к SQL Server и сетевые параметры
(`Urls`, `AllowedHosts`). Обе строки подключения используют `Trusted_Connection=True` —
**SQL-пароль в конфиг не пишется** (ADR-49). NTFS ACL всё равно ограничивают чтение
файла (см. §4).

### Правило брандмауэра

Входящее TCP-правило `MitLicense Center` для выбранного порта открывается через
`netsh advfirewall firewall add rule` — **только для профилей Domain/Private** (SEC-06),
не для профиля Public (недоверенные сети). Для деплоя за реверс-прокси правило можно
не открывать вовсе, привязав Kestrel к localhost — см. firewall/bind в `OPERATIONS.md`.

### Запись меню «Пуск»

Интернет-ярлык `%ProgramData%\Microsoft\Windows\Start Menu\Programs\MitLicense Center\
MitLicense Center.url` — открывает панель в браузере по умолчанию.

### Запись IIS

Панель **не** регистрирует собственный сайт IIS. Kestrel-служба работает как
самостоятельный веб-сервер на выбранном TCP-порту. IIS используется панелью как
управляемый объект (публикации инфобаз через `webinst.exe`), но не как хост
самой панели.

---

## 3. Мастер установки: страницы по коду

Мастер реализован в секции `[Code]` файла `installer/MitLicenseCenter.iss`.
Тело `[Code]` разнесено по модулям `installer/include/*.iss` (подключаются директивами
`#include` под `[Code]`; главный файл содержит карту модулей): объявления и WinAPI, аксессоры
ввода, управление службой, экранирование/строки подключения, генерация конфигурации и NTFS-ACL,
провижининг SQL/регистрация службы, тест подключения, построение страниц мастера.
Страницы перечислены в порядке отображения.

### 3.1 Страница выбора каталога (стандартная Inno)

Стандартный экран выбора целевого каталога (`wpSelectDir`).
По умолчанию: `%ProgramFiles%\MitLicense Center`.

### 3.2 Резервная копия перед обновлением (только апгрейд)

Показывается **только** при обновлении поверх существующей установки
(`not ServiceExists` → страница пропускается на чистой установке).

Текст экрана требует подтвердить, что оператор сделал резервную копию:

1. Базы данных SQL Server панели.
2. Каталога ключей шифрования `%ProgramData%\MitLicenseCenter` (key ring).

Кнопка «Далее» заблокирована, пока не установлен чекбокс:
«Я создал резервную копию базы данных SQL Server и каталога ключей шифрования».

### 3.3 SQL Server

Два поля ввода:

| Поле | По умолчанию | Пример |
|---|---|---|
| Экземпляр SQL Server | `.` | `.\SQLEXPRESS`, `СЕРВЕР\ЭКЗЕМПЛЯР` |
| Имя базы данных | `MitLicenseCenter` | любое допустимое имя |

База создаётся автоматически при первом старте службы, если её ещё нет (EF Core
миграции, `fail-fast` при старте).

Оба поля обязательны — пустое значение блокирует переход «Далее».

### 3.4 Подключение установщика к SQL (MLC-171)

Радиокнопки (взаимоисключающие) — под какой `sysadmin`-личностью **установщик** один раз
подключается к SQL, чтобы создать Windows-логин учётной записи службы (всегда
`CREATE LOGIN … FROM WINDOWS` + роль `sysadmin`) и проверить достижимость инстанса. Это
**ортогонально** выбору учётной записи службы (§3.5) — создаётся всегда Windows-логин:

- **Под учётной записью администратора, запустившего установку (Integrated Security) —
  рекомендуется.** Ввод не требуется. Работает и на экземплярах **«только
  Windows-аутентификация»**. Подходит, когда запускающий администратор сам `sysadmin` на SQL.
- **SQL-логином с ролью `sysadmin`** (например `sa`) — поля **логина** и **пароля**. Для
  экземпляров в **смешанном режиме**, когда запускающий администратор не является `sysadmin`
  на SQL. Замечания:
  - спрашивается «SQL-логин с ролью `sysadmin`», а не именно `sa` — `sa` часто отключён;
    подойдёт любой SQL-логин с ролью `sysadmin`;
  - работает **только** на экземпляре в смешанном режиме (разрешена SQL-аутентификация); на
    «только Windows-аутентификация» используйте Integrated Security.

По умолчанию выбран вариант **Integrated Security**. Указанные учётные данные используются
**однократно** и **не сохраняются**: служба в любом случае подключается к SQL по
`Trusted_Connection`, а SQL-пароль в конфигурацию не пишется (см. §4 / SECURITY.md §6).

Кнопка **«Проверить подключение»** запускает временный PowerShell-скрипт
(`System.Data.SqlClient`) и проверяет достижимость инстанса под **выбранной личностью**
(той же, которой пойдёт создание логина службы). Строка подключения (с паролем SQL-логина,
если он выбран) передаётся через временный `.ps1`-файл (не через командную строку), файл
удаляется сразу после проверки. Кнопка «Далее» заблокирована до успешного прохождения теста;
для варианта «SQL-логин» дополнительно требуются непустые логин и пароль.

### 3.5 Учётная запись службы (ADR-49)

Радиокнопки (взаимоисключающие) — под какой учётной записью работает служба; к SQL во всех
случаях используется `Trusted_Connection=True`:

- **Виртуальная учётная запись `NT SERVICE\MitLicenseCenter` (рекомендуется)** — без пароля,
  для рабочей группы и домена. Ввод учётных данных не требуется. Установщик сам создаёт
  SQL-логин этой учётки с ролью `sysadmin`.
- **Указанная учётная запись Windows / gMSA** — для доменных сценариев
  (`ДОМЕН\Пользователь` или `ДОМЕН\Имя$` для gMSA).

По умолчанию выбрана **виртуальная учётная запись**. Описание на странице явно указывает,
что SQL-логин и пароль в конфигурации больше не используются.

### 3.6 Учётные данные (только для именованной учётки / gMSA)

Страница показывается **только** при выборе именованной учётной записи (для виртуальной
учётки пропускается — `ShouldSkipPage`). Два поля и чекбокс:

- «Windows-аккаунт / gMSA (ДОМЕН\Пользователь или ДОМЕН\Имя$)» / «Пароль».
- Чекбокс **«Это групповая управляемая учётная запись (gMSA) — без пароля»**: при отметке
  поле пароля игнорируется и служба регистрируется без `password=`. Пароль обязателен только
  для обычной (не-gMSA) учётки.

Тест подключения к SQL на этой странице **не дублируется** — он выполняется на странице
«Подключение установщика к SQL» (§3.4) под выбранной личностью провижининга (и работает для
обоих режимов учётки службы, включая виртуальную, у которой этой страницы нет).

### 3.7 Сеть

| Поле | По умолчанию | Описание |
|---|---|---|
| TCP-порт панели | `8080` | Порт Kestrel (`http://+:<порт>`) и входящего правила брандмауэра |
| AllowedHosts | `*` | Допустимые имена хостов в заголовке `Host`; `*` — любой |

Мастер проверяет, что порт — число в диапазоне 1–65535, и что поле `AllowedHosts`
не пустое.

При переходе «Далее» с этой страницы мастер проверяет целевую базу данных на наличие
существующих учётных записей панели (`auth.Users`). Если БД уже инициализирована —
страница задания пароля администратора (§3.8) пропускается, оператор получает
информационное предупреждение.

### 3.8 Учётная запись администратора (только чистая установка)

Показывается только при чистой установке на пустую базу данных. Пропускается:

- при обновлении (admin уже существует в БД);
- при чистой установке поверх уже инициализированной БД.

| Поле | Описание |
|---|---|
| Пароль администратора | Логин фиксированный: `admin` |
| Подтверждение пароля | Должно совпадать с первым полем |

Требования к паролю (проверяются в мастере и повторно сидером при первом старте):

- не менее **12 символов**;
- содержит **заглавную** и **строчную** буквы;
- содержит **цифру**;
- содержит **спецсимвол** (любой не-буквенно-цифровой символ).

### 3.9 Подтверждение и установка (стандартная Inno)

Стандартный экран «Готово к установке» с кратким итогом выбранных параметров.

---

## 4. Учётные записи и права

### Права в SQL Server

Учётная запись службы должна иметь роль **`sysadmin`** на экземпляре SQL Server. Это
необходимо для:

- создания базы данных при первом старте (если база не существует);
- применения EF Core миграций (DDL);
- создания схем `auth`, `dbo`, `hangfire`.

**SQL-логин учётки службы создаёт сам установщик** (`CREATE LOGIN [<учётка>] FROM WINDOWS`
+ `ALTER SERVER ROLE sysadmin ADD MEMBER`, идемпотентно, процедура `ProvisionSqlLogin` в
`ssPostInstall`; **всегда Windows-логин**). Провижининг выполняется под `sysadmin`-личностью,
выбранной на странице «Подключение установщика к SQL» (§3.4): Integrated Security
администратора-установщика (по умолчанию) **или** введённый SQL-логин с ролью `sysadmin` (для
смешанного режима). Поэтому достаточно, чтобы установщику была доступна **какая-либо
`sysadmin`-личность** (предусловие §1). Введённый SQL-логин транзиентен — используется
однократно и **не сохраняется** (в конфиг не пишется; через временный `.ps1`, не в командной
строке). На провале установщик показывает критическую ошибку с указанием обеих возможных
причин (запускающий админ не `sysadmin` / SQL-логин неверен либо не `sysadmin` либо инстанс не
в смешанном режиме) и **откатывает установку** (а не оставляет службу, падающую на старте с 18456).

### Права учётной записи службы (ADR-49)

- Право **«Вход в качестве службы»** (`SeServiceLogonRight`) — SCM выдаёт его
  автоматически при `sc create obj=…` на валидной учётке (для виртуальной учётки
  `NT SERVICE\MitLicenseCenter` — гарантированно). Если SCM не смог выдать право —
  служба не запустится; право выдаётся вручную через `secpol.msc` → «Локальные
  политики» → «Назначение прав пользователя» → «Вход в качестве службы».
- Членство в **локальной группе Administrators** — нужно для управления IIS (публикации
  через `webinst.exe`/`Microsoft.Web.Administration`, recycle/iisreset). **Установщик
  добавляет учётку в группу автоматически** по well-known SID `S-1-5-32-544`
  (`Add-LocalGroupMember`, процедура `AddServiceAccountToAdministrators`, best-effort).

### Пост-установочный чек-лист (REL-06)

После завершения мастера установки выполните следующие проверки:

1. **Служба запустилась и не упала.** Откройте `services.msc` → `MitLicense Center` → статус
   «Выполняется». Если служба упала — причина в Журнале событий Windows (журнал «Приложение»,
   источник `MitLicenseCenter`). Ошибка «Login failed for user» (18456) означала бы, что у
   учётки службы нет прав в SQL — но при штатной установке её SQL-логин создаёт сам установщик
   (см. выше), поэтому 18456 указывает на ручное вмешательство или сбой провижининга.

2. **IIS-операции работают.** Откройте панель → раздел «Публикации» → проверьте, что статус
   публикаций не «Ошибка проверки». «Ошибка проверки» означает, что учётка службы не входит в
   локальную группу Administrators (авто-добавление установщиком — best-effort и могло не
   пройти). Добавьте её вручную: `lusrmgr.msc` либо
   `Add-LocalGroupMember -SID S-1-5-32-544 -Member "NT SERVICE\MitLicenseCenter"`.

3. **Право «Вход в качестве службы».** Если служба не создалась (ошибка `sc.exe 1057`) — обычно
   опечатка в имени именованной учётки/gMSA или неверный пароль; выдайте право вручную:
   `secpol.msc` → «Локальные политики» → «Назначение прав пользователя» →
   «Вход в качестве службы» → добавить учётку.

### NTFS ACL: каталог `%ProgramData%\MitLicenseCenter`

Устанавливается через `icacls` в процедуре `HardenDataDirAcl` в `ssPostInstall`.

Что делает инсталлятор:
1. `/inheritance:r` — снимает наследование ACL от `%ProgramData%` (где по умолчанию
   `Users:RX`).
2. Добавляет явные ACE с наследованием на дочерние объекты `(OI)(CI)`:
   - `NT AUTHORITY\SYSTEM` — полный доступ (`F`);
   - `BUILTIN\Administrators` — полный доступ (`F`);
   - **учётная запись службы** (`NT SERVICE\MitLicenseCenter` или именованная/gMSA) — `Modify` (`M`).

Грант учётке службы безусловный (ADR-49). SYSTEM/Administrators — в SID-нотации
(`*S-1-5-18`, `*S-1-5-32-544`), независимо от локали Windows; учётка службы — по имени
`NT SERVICE\…` (префикс не локализуется). Грант возможен только **после** `sc create`
(имя/SID учётки уже существуют) — отсюда порядок шагов `ssPostInstall` (см. §6 / ADR-49).

ACL идемпотентны: накатываются и при чистой установке, и при апгрейде.

Сбой `icacls` выводит предупреждение с путём и кодом ошибки, **не прерывает
установку**: служба работоспособна, но защита секретов на диске не применена.
Оператор может выполнить ужесточение вручную.

### NTFS ACL: `{app}\appsettings.Production.json`

Устанавливается через `HardenConfigAcl` сразу после записи файла.

1. `/inheritance:r` — снимает наследование от `%ProgramFiles%` (где `Users:RX`).
2. `NT AUTHORITY\SYSTEM` и `BUILTIN\Administrators` — полный доступ.
3. **учётная запись службы** — только чтение (`R`).

Файл использует `Trusted_Connection` и **SQL-пароля не содержит** (ADR-49); ACL всё равно
ограничивают чтение SYSTEM/Administrators + учёткой службы (defense-in-depth).

---

## 5. Первый вход: механизм `initial-admin.secret` (ADR-31)

### Как формируется пароль

При чистой установке на пустую базу мастер записывает заданный оператором пароль в
файл:

```
%ProgramData%\MitLicenseCenter\initial-admin.secret
```

Файл записывается **до** ужесточения ACL каталога (`HardenDataDirAcl`), чтобы
`(OI)(CI)` ACE накрыло и этот файл — учётка службы получает `Modify` и может его
прочитать и удалить при первом старте.

### Что делает бэкенд при первом старте

1. Читает `initial-admin.secret`.
2. Создаёт пользователя с логином `admin` и прочитанным паролем.
3. **Удаляет файл** `initial-admin.secret`.

После этого файл не существует.

### Если файл не был записан

Если `SaveStringToFile` завершился с ошибкой (сообщение в мастере), бэкенд создаёт
первого администратора со **случайным паролем** и записывает его в **Журнал событий
Windows** (источник `MitLicenseCenter`, журнал `Приложение`).

### Вход и смена пароля

После установки:

1. Откройте панель: `http://localhost:<порт>/` (или URL с нужным именем хоста).
2. Войдите как `admin` с паролем, заданным в мастере.
3. Смените пароль через меню профиля — раздел «Сменить пароль» (страница `/profile`).

**Первого администратора принудительная смена пароля НЕ затрагивает.** Механизм
форс-смены в продукте есть (`AppUser.MustChangePassword`; вход с этим флагом
блокируется экраном смены пароля до её выполнения), но `IdentitySeeder` не
выставляет флаг первому администратору: пароль ему задал сам оператор в мастере
(или он сгенерирован случайно и записан в Журнал событий — см. выше), поэтому
смена для `admin` остаётся добровольной.

Принудительная смена применяется только к учётным записям, созданным или сброшенным
позже через раздел «Администраторы» панели: им выдаётся временный пароль, и при
первом входе панель требует его сменить.

---

## 6. Обновление поверх существующей версии

Запустите новый файл `MitLicenseCenter-Setup-<версия>.exe`. Установщик автоматически
определяет наличие предыдущей установки по AppId Inno (`B7E9F3A2-…`).

### Порядок действий установщика при апгрейде

1. **Страница-предупреждение о бэкапе (§3.2)** — обязательное подтверждение чекбокса;
   без него кнопка «Далее» заблокирована.

2. **Остановка службы** (процедура `PrepareToInstall`, до замены файлов):
   установщик вызывает `ControlService(SERVICE_CONTROL_STOP)` и ждёт фактической
   остановки до ~30 секунд (60 итераций по 500 мс). Ожидание необходимо, чтобы
   снять блокировку с `MitLicenseCenter.Web.exe` перед копированием новой версии.

3. **Замена файлов** (секция `[Files]`, `ignoreversion`).
   `appsettings.Production.json` **не перезаписывается** (`skip-if-exists` на апгрейде —
   проверка `ServiceExists and FileExists`): правки оператора сохраняются.

4. **`ssPostInstall`** — последовательность после копирования (порядок ADR-49,
   процедура `CurStepChanged`):
   - `WriteProductionConfig` — пропускает запись конфига (файл уже существует).
   - `WriteInitialAdminPassword` — пропускает (апгрейд: admin уже создан).
   - `netsh delete rule` — снимает старое одноимённое правило брандмауэра.
   - `CreateStartMenuShortcut` — обновляет ярлык меню «Пуск».
   - `RegisterService` → `sc config` — выравнивает `binPath` и учётную запись под текущий
     выбор мастера (без пароля для виртуальной/gMSA); ошибка — предупреждение (не прерывает).
   - `ProvisionSqlLogin` — создаёт/подтверждает SQL-логин учётки (идемпотентно; под выбранной
     на §3.4 личностью провижининга — Integrated Security или SQL-логин; жёсткий fail с откатом,
     если ни одна `sysadmin`-личность недоступна).
   - `AddServiceAccountToAdministrators` — подтверждает членство учётки в Администраторах
     (идемпотентно, best-effort).
   - `HardenConfigAcl` / `HardenDataDirAcl` — накатывают ACL (грант учётке службы; идемпотентно).
   - `StartServiceAndFinalize` → recovery-политика + SQL-зависимость + `netsh add rule` +
     `sc start` (последним); `rc=1056` (`ALREADY_RUNNING`) считается успехом.

5. **Применение миграций БД** — при первом старте новой версии бэкенд самостоятельно
   применяет новые EF Core миграции (`fail-fast`, ADR-18). Если миграции провалились
   и службу не удалось запустить — финальный экран мастера показывает **ошибку**
   (красный заголовок «Установка завершена с ОШИБКОЙ»), а не «успех».

### Что сохраняется при апгрейде

| Что | Результат |
|---|---|
| `appsettings.Production.json` | Сохраняется (не перезаписывается) |
| `%ProgramData%\MitLicenseCenter` (key ring) | Не трогается |
| База данных SQL Server | Не трогается |
| Учётные записи панели | Не трогаются |

### Если старт службы провалился

Финальный экран мастера показывает ошибку (красный заголовок). Причину смотреть
в **Журнале событий Windows** (журнал «Приложение», источник `MitLicenseCenter`):
бэкенд пишет туда причину отказа (`fail-fast`, ADR-18) — недоступность SQL,
ошибка миграций, неверные права учётной записи службы и т.п.

Для запуска службы вручную после устранения причины: `services.msc` или
`sc start MitLicenseCenter`.

Если миграции применились частично — восстановить базу данных и key ring из
резервной копии, сделанной на шаге §3.2.

---

## 7. Удаление

Удаление запускается через «Программы и компоненты» (appwiz.cpl) или повторным
запуском установщика.

### Что удаляется автоматически

Секция `[UninstallRun]`:
- Остановка службы: `sc stop MitLicenseCenter`.
- Удаление службы: `sc delete MitLicenseCenter`.
- Удаление правила брандмауэра: `netsh advfirewall firewall delete rule name="MitLicense Center"`.

Секция `[UninstallDelete]`:
- Каталог ярлыка меню «Пуск»: `%ProgramData%\Microsoft\Windows\Start Menu\Programs\MitLicense Center`.
- Файл `{app}\appsettings.Production.json` (сгенерирован мастером вне секции `[Files]`).
- Остальные файлы каталога установки (`{app}`) удаляются штатным механизмом Inno.

### Каталог данных — по выбору оператора

При удалении появляется диалог (кнопка «Нет» выбрана по умолчанию):

> «Удалить конфигурацию и КЛЮЧИ ШИФРОВАНИЯ из  
> `%ProgramData%\MitLicenseCenter` ?  
> Нет (рекомендуется) — сохранить для переустановки.  
> Да — удалить ключи. ВНИМАНИЕ: без них секреты в базе данных (dbo.Settings)
> расшифровать НЕЛЬЗЯ.»

- **Нет (по умолчанию)** — каталог `%ProgramData%\MitLicenseCenter` с key ring
  сохраняется. Key ring и БД — единый бэкап-юнит (ADR-8): без ключей секретные
  настройки в `dbo.Settings` не расшифровать.
- **Да** — каталог удаляется (`DelTree`). Выбирать только если БД тоже выводится
  из эксплуатации.

### Что не удаляется никогда

| Что | Почему |
|---|---|
| База данных SQL Server | Установщик не знает крединалов SQL на момент деинсталляции; удаляется вручную через SSMS или `sqlcmd` |
| `%ProgramData%\MitLicenseCenter` | По умолчанию сохраняется (дефолт диалога — «Нет») |

---

## Приложение: диагностика проблем запуска

Установщик записывает лог в `%TEMP%\Setup Log *.txt` (`SetupLogging=yes` в `.iss`).

Если служба не стартует после установки или обновления:

1. Открыть **Журнал событий Windows** → журнал «Приложение» → источник
   `MitLicenseCenter`. Бэкенд пишет причину отказа при старте.
2. Проверить подключение к SQL Server — инстанс, имя БД, наличие SQL-логина учётной
   записи службы с ролью `sysadmin` (создаётся установщиком автоматически).
3. Проверить право «Вход в качестве службы» учётки (`secpol.msc`).
4. Проверить лог установщика: `%TEMP%\Setup Log *.txt`.
5. Убедиться, что порт не занят другим процессом:
   `netstat -ano | findstr :<порт>`. **(не проверено на стенде)**
