# MitLicense Center — Project Backlog

Единый активный реестр задач. Этот файл **постоянно поддерживается в актуальном состоянии**
и читается первым при каждом следующем запуске работы над проектом.

Закрытые задачи перечислены **компактным индексом** в `docs/PROJECT_BACKLOG_ARCHIVE.md`
(одна строка на задачу: суть + дата + якорь-коммит); полные постановки и отчёты несёт
git-история (коммиты `MLC-NNN:` и PR) — детали достаются по инструкции в шапке архива.
Этот файл и архив держим тонкими, чтобы не жечь контекст при каждом старте. Канон проекта
(`docs/01..06 + DECISIONS.md + ROADMAP.md + OPERATIONS.md`) —
источник правды по архитектуре v1. Бэклог не дублирует канон, а фиксирует улучшения поверх него.

## Как пользоваться

1. Прочитать этот файл.
2. Найти задачу, помеченную `NEXT TASK`.
3. Выполнить **только её**.
4. После выполнения задачи: статус `Done`, **полный отчёт — в описании закрывающего PR
   и в теле коммита** (не в архив-файл — он остаётся тонким индексом); в секции трека здесь
   остаётся **сжатая Done-строка** (1–3 строки: что построено + гочи, нужные *следующим
   задачам этого трека*) — передаточная записка, не летопись.
5. После закрытия трека: куратор добавляет в `PROJECT_BACKLOG_ARCHIVE.md` по **одной
   строке-индексу на каждую задачу трека** (суть + дата + якорь-коммит, как в шапке архива)
   и удаляет секцию трека из реестра; в индексе «Завершённые треки» остаётся одна строка,
   актуализируется счётчик в «Закрыто». Развёрнутые отчёты живут в git, не в архив-файле.
6. Не выполнять больше одной задачи за сессию без отдельного указания.

Цель схемы: реестр читается при старте каждой сессии и должен оставаться ~150–200 строк
независимо от возраста проекта — растёт во время трека, схлопывается при его закрытии.

При обнаружении расхождения кода и документации — не исправлять автоматически:
завести запись `[Doc divergence]`, описать расхождение, предложить варианты.

## Когда заводить запись

Раздел «Как пользоваться» выше — про **исполнение** уже существующих записей; этот —
про **наполнение** реестра. Запись заводят, когда работа выходит за рамки текущей
активной задачи.

**Заводить запись** (кандидаты реестра):
- дефект корректности / безопасности / производительности, найденный вне текущей задачи;
- техдолг или улучшение поддерживаемости с понятным ROI (меньше дублирования / дешевле
  расширять / меньше дрейфа), не являющийся дефектом;
- расхождение код↔документация — обязательно как `[Doc divergence]` с вариантами,
  **не** чинить молча.

**Не заводить запись** (чинить на месте или не трогать):
- тривиальная мелочь по ходу текущей `NEXT TASK`, входящая в её объём, — правится сразу;
- вкусовщина без ROI, гипотезы «можно было бы красивее» без измеримой выгоды;
- то, что уже покрыто каноном `docs/` или `CLAUDE.md`, — в реестре не дублировать.

**Кто создаёт.** Net-new записи и `NEXT TASK` ведёт внешний чат-куратор (держит порядок
исполнения и выдаёт постановку). Чат-исполнитель закрывает одну задачу за сессию и
`NEXT TASK` сам не выставляет; найденные по ходу кандидаты — фиксирует записью и
отчитывается куратору, не берёт их в тот же ход.

## Формат записи

`ID` · `Category` (Architecture / Security / Performance / Maintainability / Testing /
Frontend / Backend) · `Priority` (P1 / P2 / P3) · `Severity` (Critical / High / Medium /
Low) · `Module` · `File(s)` · `Description` · `Impact` · `Recommendation` · `Status`.

**Статусы:** `Open` · `Approved` · `In Progress` · `Done` · `Rejected`.

**Приоритеты:** `P1` — исправить обязательно · `P2` — желательно · `P3` — можно отложить.

## Общая оценка

Зрелый, production-ready проект. Аудит безопасности и дефектов (MLC-001..019, 2026-06-01..03)
**закрыт** — критичных дыр нет, отчёты в `PROJECT_BACKLOG_ARCHIVE.md`.

Реестр техдолга и поддерживаемости (MLC-020..028, анализ 2026-06-03) — **закрыт**: MLC-020..024
выполнены, MLC-025..028 переведены в отложенные опции (брать по триггеру). Это **не** были дефекты
корректности/безопасности (поэтому нет P1), а снижение стоимости будущего развития. Архитектурный
фундамент крепкий (строгие adapter-границы к 1С/IIS, чистые статические хелперы, docs-as-canon) —
менять его не нужно.

**Рефакторинг-трек (2026-06-03)** — план долгосрочного улучшения поддерживаемости, полные спеки
в локальном план-файле `distributed-orbiting-snail.md` (вне репозитория; ID `REF-01..REF-13`). Phase 1–2
(`MLC-029..035`) **закрыт полностью**; Phase 3–4 (REF-08..13 → `MLC-025/026/027/011(a)/028/036`)
остаётся gated на триггеры — см. «Открытые опции».

---

## NEXT TASK

### Трек — Релиз 1.1.0 (расширенный)

Минорный фичебатч поверх `v1.0.0`. **Этап 1 — фокус-ядро `MLC-177..184`** (5 пунктов роадмапа +
stand-фидбэк 182/183/184): влит в `main`, pre-release `<Version>`=`1.1.0-beta` + тег `v1.1.0-beta`
нарезан (клиенты pre-release не видят — ADR-50), **на стенде владельцем пройден ✅ (2026-06-17)**.

**Этап 2 (решение владельца 2026-06-17) — доделать в ЭТОЙ ЖЕ версии 3 оставшихся пункта роадмапа**
(раньше планировались в 1.2.0): размер баз MSSQL, редизайн «Обзора», ESLint StatusBadge — `MLC-185..187`
ниже. Ведёт **новый чат-куратор** по кураторской модели (план-режим → постановка → субагент → ревью → merge).

**Промоут до `v1.1.0` — ТОЛЬКО после этапа 2 + повторного стенда** (бамп `<Version>`→`1.1.0` + тег
`v1.1.0`; опц. публикация обычного GitHub Release). НЕ промоутить раньше. Полная архивация трека
(индекс в `PROJECT_BACKLOG_ARCHIVE.md`, удаление секции) — после промоута.

#### Этап 2 — добор роадмапа в 1.1.0 (за новым чатом)

| MLC | Роадмап | Суть | Статус |
|---|---|---|---|
| 185 | размер баз | Мониторинг размера баз MSSQL + отчёт по динамике (BE+FE). Питает Обзор (186). | **Done** (a–g) |
| 186 | редизайн Обзора | Живой «Обзор» + полировка под shadcn (FE). Нужны **дизайн-эскизы ДО кода** (виджеты: тренды/алерты/динамика лицензий и размеров баз). Зависит от 185; обсудить дизайн с владельцем (как bulk-бар в 184). | **NEXT TASK** (дизайн) |
| 187 | ESLint | Правило-линтер: запрет «сырых» цветовых классов статусов мимо `StatusBadge` (FE-инфра, низкий приоритет). Независим. | Open |

Порядок: **185 → 186** (186 использует данные 185); **187** — в любой момент. ROADMAP несёт развёрнутые
постановки этих трёх (там же границы: ADR-28 single-host для размера баз; «уже на shadcn» для Обзора).

**`MLC-185` Done** (2026-06-17, дроблён a–g, PR #309–315). Мониторинг размера баз MSSQL. Решения владельца
(заморожены): метрики = data+log РАЗДЕЛЬНО, занятое на диске (allocated, один проход `sys.master_files`, НЕ
used/`FILEPROPERTY`); каданс = суточный ночной Hangfire-CRON (`0 2`); охват = ТОЛЬКО базы инфобаз
(`Infobase.DatabaseName`), «итог по хосту» = сумма по ним; хранение = `Setting DatabaseSize.RetentionDays`
(default 365, выведена в /settings). **Единицы — КБ/МБ/ГБ** (общий `@/lib/formatBytes`; отступление от
первоначального МиБ/ГиБ ради консистентности со всем UI). Аудита нет (enum заморожен). Состав: **185a**
сущность `DatabaseSizeSnapshot` + миграция + Setting; **185b** порт `IDatabaseSizeProbe` + ADO.NET-адаптер
(`sys.master_files`) + фейк; **185c** джобы сбора+ретеншена (близнец лицензионной, MLC-074) + канон джоб;
**185d** колонка «Размер» в «Базах» (последний снимок, omit-null); **185e** BE отчёта `/reports/database-size`
(+ drilldown, зеркало лицензий); **185f** FE-вкладка «Размер баз» (recharts + таблицы + drilldown); **185g**
экспорт CSV/Excel/HTML/PDF. Гейт: FE 744/744 + type-check; BE 1022/1030 (8 падений — пред-существующий
средовой ACL DataProtection, не регресс). Спека — план-файл `mlc-185-database-size-monitoring.md`. **Живая
семантика `sys.master_files` (реальные размеры, ночной сбор) — за владельцем на стенде этапа 2.**

**`MLC-186` (NEXT — фаза дизайна):** редизайн «Обзора». Сначала **дизайн-эскизы и обсуждение с владельцем**
(как bulk-бар в 184), потом постановка/код. Данные 185 (динамика размеров баз) + лицензий (`/reports`) +
аудит/здоровье — кандидаты в виджеты. Канон `06_UI_GUIDE` правится тем же PR.

| MLC | Роадмап | Суть | Статус |
|---|---|---|---|
| 177 | #6 | Отчёты: локальные сутки (00:00–24:00 браузерной зоны) + оговорка свежести 15-мин | **Done** |
| 178 | #2 | Список бэкапов сверяется с фактом `.bak` на диске | **Done** |
| 179 | #4 | Полнота журнала аудита (write-эндпоинты + enforcement; enum заморожен) | **Done** |
| 180 | #5 | Удаление пользователя (`DELETE /users/{id}`, guard'ы, аудит `UserDeleted`) | **Done** |
| 181 | #3 | Унификация списков: поиск / searchable-select / bulk-by-filter (a/b/c) | **Done** |

**`MLC-177` Done** (2026-06-17) — отчёты: дефекты часового пояса и свежести. FE-only: `reportsUrlState`
трактует date-only как локальные сутки браузера → UTC-границы на проводе (DST-safe, нативный
`new Date(y,m,d,…)`), ось графика уже локальная → период и ось сошлись встык (приведение в соответствие
ADR-23); i18n-пометка «время браузера» + оговорка про возможное отставание последнего 15-мин интервала.
Контракт API не тронут — parity цела; Zod/бэкенд не менялись. Тесты прибивают TZ (`Europe/Moscow`).
Канон — нота к **ADR-23**.

**`MLC-178` Done** (2026-06-17) — список бэкапов сверяется с фактом на диске. (A) живой флаг
`fileAvailable` в `BackupSummary`: батч server-side `xp_fileexist` (новый метод порта
`ISqlBackupService.FilesExistAsync`, «never throws» → `null`=«не проверили»), проверяются только
`Succeeded`+путь; FE помечает `false` как «файл отсутствует». (B) узко: `CompleteAsync` обнуляет
`FilePath`/`FileSizeBytes` вытесненных keep-latest строк (история цела, **строки не удаляются →
аудит-enum не тронут**). Parity: `fileAvailable` в Zod (`omittable`). Канон — нота к **ADR-27**.
Гоча: реальный `xp_fileexist` в unit не гоняется (фейк `FakeSqlBackupService.ExistingFilePaths`);
семантика — на стенде.

**`MLC-179` Done** (2026-06-17) — полнота аудита: ревизия показала зрелое покрытие, единственный
пробой — записи «Быстродействия». Заведена новая **700-серия** (`PerfRecordingStarted=700`/
`Stopped=701`/`Deleted=702`, host-scope `TenantId=null`, пишется только при фактическом успехе);
механика — `httpContext.AuditAsync` + фабрики `AuditDescriptions`. Parity: i18n `audit.actions` +
union `AuditActionType` + каталог `AUDIT_ACTION_TYPES`; golden-тест frozen-int (700–702) + зеркала.
Download бэкапа/экспорт отчётов — эндпоинтов нет, аудировать нечего. Канон — ADR-26 уточнён, enum-
список 03/04, исправлен устаревший комментарий в `IPerfRecordingService`. **109 оставлен под `UserDeleted` (MLC-180).**

**`MLC-180` Done** (2026-06-17) — жёсткое удаление учётки `DELETE /users/{id}` (Admin,
`UserManager.DeleteAsync`). Переиспользует guard'ы отключения (нельзя себя; нельзя последнего
активного админа); аудит `UserDeleted=109` только при успехе; жёстких FK на пользователя нет —
история (аудит/бэкапы/perf) ссылается строкой и переживает удаление. Подтверждение — FE-барьер
ADR-45 (`DeleteUserDialog`, без ввода имени), серверного `Confirm`-гейта нет. «Отключить» остаётся.
Parity: аудит-зеркала (i18n+union+каталог+golden 109). Канон — нота к **ADR-37**, enum 03/04.

**`MLC-181` Done** (2026-06-17) — унификация управления списками, три под-PR: **181a** серверный поиск
по «Базам» (`Name||DatabaseName`, plain `Contains`/CA1862-паттерн как у Клиентов/Бэкапов; гоча:
StringComparison не транслируется, InMemory маскирует); **181b** единый `SearchableSelect` клиента в
тулбаре «Баз» + формах (компонент существовал, +проп `disabled`); **181c** bulk «Выбрать все N по
фильтру» — вариант A: read-only `GET /infobases/ids` по фильтру (кэп 5000, `capped`) + существующий
per-id движок (безопаснее серверного bulk-by-filter по ADR-45). Ключ корректности 181c — **общий
`BuildFilteredQueryAsync`** для `ListAsync` и `ids` (выбор = ровно то, что в списке). Усилено
подтверждение (N + сводка фильтра, без type-to-confirm — действия обратимы). **ROADMAP #3 закрыт.**

**Stand-фидбэк 1.1.0-beta (follow-up к MLC-178, найдено владельцем на стенде):**
**`MLC-182` Done** (2026-06-17, FE-only) — список бэкапов различает два «нет файла»: `Succeeded`+пустой
`filePath` → спокойное «вытеснен» (keep-latest, норма); `Succeeded`+`fileAvailable=false` → заметное
«файл отсутствует» (неожиданно пропал). Раньше вытесненные показывали немое «—» (двусмысленно с
«размер неизвестен»). Бэкенд не тронут (данные уже различимы). Канон — нота к ADR-27.
**`MLC-183` Done** (2026-06-17, BE+FE) — предпоказ disk-guard в окне бэкапов: «свободно · оценка
~… (до сжатия)» ДО запуска. Disk-guard вынесен из `BackupAsync` в `ISqlBackupService.EstimateAsync`
(одна реализация — и стоп-кран, и предпоказ); read-only `GET /backups/estimate` (Viewer); ненастроено/
нет прав → 200 degraded. При нехватке — предупреждение, кнопку НЕ блокируем (оценка — верхняя граница,
серверный guard остаётся стоп-краном). Переиспользован `BackupFailureReason` (enum не трогаем), общий
кламп `SettingDefinitions.ClampToRange`. Канон — нота к ADR-27. Гоча: реальные `xp_*` в unit не гоняются (фейк).
**`MLC-184` Done** (2026-06-17, дроблён a/b) — «Базы», по фидбэку стенда. **184a** (BE): фикс 500 в
`GET /infobases/ids` («Выбрать все по фильтру») — `OrderBy` шёл по членам спроецированного record
(EF на живом SQL не транслирует; InMemory маскировал) → сортировка по `Infobase.Name`/`Id` ДО
проекции (как `ListAsync`); тест на **SQLite** (red-on-old подтверждён). **184b** (FE): редизайн
`PublicationsBulkBar` — зона выбора (счётчик + «выбрать все N по фильтру» + снять) отделена от зоны
действий (Опубликовать/Сменить платформу + меню «Ещё»: Проверить, Снять с публикации, Удалить базу);
3 новых bulk-диалога на `useBulkOperation`; удаление бьёт по инфобазе (map publicationId→infobaseId,
чекбокс «снять из IIS»), подтверждения по ADR-45 (delete — необратимость, unpublish — да/нет).
Бэкенд/контракты/аудит-enum не тронуты. Канон — `05_FRONTEND` (панель массовых действий).

**`MLC-176` Done** (2026-06-16, **ADR-50**) — проверка обновлений через
GitHub Releases: панель ненавязчиво сигналит о новой версии. Ленивая сверка `latest`-релиза
публичного репо (`Updates.Repository`, анонимно) с версией сборки через свой semver-компаратор
(`Domain/Updates`, release > prerelease); первый в проекте типизированный `HttpClient`
(`IGitHubReleaseClient`, обяз. `User-Agent`, любой сбой → «проверка недоступна»); кэш `IMemoryCache`
TTL `Updates.CheckIntervalHours` (6 ч; недоступно → 5 мин), рубильник `Updates.Enabled`. Эндпоинты
`GET /updates/status` (Viewer) + `POST /updates/check-now` (Admin). UX = уведомление: баннер
«Доступна версия X.Y.Z» всем + «Открыть релиз» всем + «Скачать установщик»/«Проверить сейчас» —
Admin; запуск установщика руками админа под UAC. Аудит не пишем (enum заморожен). Канон — ADR-50 +
`02`/`04`/`05`/`DEVELOPMENT`. Спека — `vivid-waddling-sedgewick.md`. Следующую задачу ставит куратор.
**За владельцем (живой стенд):** на узле с опубликованным новым релизом — баннер появляется, ссылки
ведут на релиз/`.exe`, «Проверить сейчас» (Admin) форсит проверку; при оффлайн-узле баннера нет.

Трек «Рефакторинг + редизайн установщика» **закрыт** (3/3, версия `0.7.0-beta`).

**За владельцем (живой стенд):** прогон `0.7.0-beta` на чистой ВМ и апгрейд-ВМ по сценариям
`MLC-175` (оба режима провижининга: Integrated / SQL-логин; оба режима учётки службы: виртуальная /
именованная+gMSA; авто-тест блокирует «Далее» при недоступном SQL; сводка на «Готово» корректна;
существующая БД → пропуск пароля admin; апгрейд → страница бэкапа + пропуск admin; негатив
Integrated-не-sysadmin → откат). Плюс непогашенное по `MLC-170/171/172` (миграция B→A `OPERATIONS.md §8`).

**Завершённые ранее (контекст):** Трек «Релиз `0.6.0-beta`» (`MLC-164..166`) **выпущен**: аудит при добавлении
базы пишет одну запись (164); `ConsumesLicense` — факт `rac --licenses`, app-id убран из подсчёта,
трёхсостояние `LicenseStatus` + пауза enforcement при сбое rac + grace-guard (166, **ADR-48**);
«Сеансы» — фильтр по типам сеансов (165). **Follow-up `0.6.1-beta` (`MLC-167`):** «Сеансы» по умолчанию
показывают полный список **фактически лицензионных** сеансов (тумблер «Только лицензионные» ВКЛ,
перекрывает типы); фильтр типов — полный каталог типов + «Выбрать все». Индекс задач —
`PROJECT_BACKLOG_ARCHIVE.md`. **Фикс `0.6.2-beta` (`MLC-168`):** отчёт «Использование лицензий» в окне
недоступности факта rac (`--licenses` упал) больше не пишет ложный 0 — холодный цикл пропускает сэмпл
(честный пробел вместо занижения min/avg). **Остаётся за владельцем:** живая проверка на стенде после
апгрейда на `0.6.2` — на «Сеансах» по умолчанию виден список потребляющих лицензию (факт 1С),
завершение лишних — по реально лицензионным. Следующую задачу/трек ставит куратор постановочным PR.
Кандидаты — «Открытые опции» ниже и `ROADMAP.md`.

## Завершённые треки — индекс

Компактный индекс закрытых задач (по треку, с якорями-коммитами) — в
`PROJECT_BACKLOG_ARCHIVE.md`; развёрнутые отчёты, методика и развилки треков — в git-истории
(коммиты `MLC-NNN:`/PR, поиск по `MLC-NNN` или названию трека); спеки — в локальных
план-файлах (вне репозитория). Здесь — только указатели.

- **Аудит безопасности и дефектов** — `MLC-001..019` (2026-06-01..03).
- **Техдолг и поддерживаемость** — `MLC-020..024` (2026-06-03); `MLC-025..028` → «Открытые опции».
- **Рефакторинг-трек Phase 1–2** — `MLC-029..035` (2026-06-03..04); спека `distributed-orbiting-snail.md`.
- **Перф-трек Phase 1–2 (PERF-01..07)** — `MLC-037..043` (2026-06-04..05); PERF-08+ gated.
- **Вне треков:** `MLC-044` hot-enforce ≤5с (2026-06-05); `MLC-045`/`MLC-046` webinst-публикации +
  bulk (2026-06-05); `MLC-047` управление IIS, ADR-24 (2026-06-06); `MLC-053` reset-admin (2026-06-07);
  `MLC-072` диагностика метрик «Быстродействия» — числа корректны (2026-06-09); `MLC-074`
  retention-джобы под execution strategy (2026-06-09).
- **Трек «Отчёты — использование лицензий»** — `MLC-048..050` 3/3 (2026-06-06), раздел `/reports`,
  ADR-25; спека `concurrent-purring-kahn.md`; + `MLC-054` полировка (2026-06-07).
- **Трек «Экспорт отчётов»** — `MLC-051..052` 2/2 (2026-06-07), CSV/XLSX/HTML/PDF;
  спека `adaptive-scribbling-quiche.md`.
- **Трек «Полировка /settings»** — `MLC-055..056` 2/2 (2026-06-07..08); спека `1-2-rippling-zephyr.md`.
- **Трек «Полировка панели v1.1»** — `MLC-057..059` 3/3 (2026-06-08), тема + «Администраторы» +
  форс-смена пароля; спека `eventual-hopping-pebble.md`.
- **Мини-трек «Раздел Пользователи»** — `MLC-060..061` 2/2 (2026-06-08), переименование + смена роли;
  спека `users-section-rename-roles.md`.
- **Трек «Анализ быстродействия 1С»** — `MLC-063..071` 9/9 (2026-06-08..09), раздел `/performance`:
  host-снимок → 1С-сеансы → SQL live + запись по требованию (ADR-26);
  спека `spicy-discovering-torvalds.md`; UX-полировка `MLC-073` → «Открытые опции».
- **Трек «Резервное копирование баз SQL»** — `MLC-075..078` 4/4 (2026-06-09..10), on-demand
  COPY_ONLY-бэкапы + keep-latest (ADR-15 изменён, ADR-27); спека `lazy-cuddling-finch.md`;
  хвосты `MLC-079`/`MLC-080` влиты в UX-трек.
- **Трек «UX-пересборка панели под single-host», этап 1** — `MLC-081..086` 6/6 (2026-06-10),
  frontend-only: «Базы» = Инфобазы + Публикации (вкладка «IIS», `/publications` удалена), форма
  без выбора сервера, /settings без «Значений по умолчанию» (секция «SQL Server»), профиль в
  топбаре + сайдбар 8 пунктов, дашборд-«Обзор» (кликабельные KPI + здоровье хоста), канон 05/06
  переписан; +влитые `MLC-079`/`MLC-080`; аудит-спека `ux-audit-single-host.md`;
  тег отката `v1-pre-ux-redesign`.
- **Трек «UX-пересборка, этап 2: single-host бек-чистка»** — `MLC-087..091` 5/5 (2026-06-10),
  по подтверждению single-host пользователем: ключ `Sql.Server` — единственный источник
  SQL-инстанса (discovery без `server=`), колонка `Infobase.DatabaseServer` и ключ
  `OneC.Cluster.Server` удалены (миграции), фильтр `publishStatus` на «Базах», **ADR-28
  «Single-host topology»** + канон 01/03/04 present-tense; хвост-тест полноты §7 пройден;
  тег `v1-pre-singlehost-stage2`, бэкапы стендовой БД — `F:\MlcStage2Backups`.
- **Трек «Нераспределённые базы: discovery-first добавление»** — `MLC-092..094` 3/3
  (2026-06-11), закрытие слепой зоны (а) дрейфа панель↔кластер: `GET /infobases/unassigned`
  (diff RAS − заведённые − скрытые, TTL-кэш 60 с), игнор-лист `HiddenClusterInfobases` +
  hide/unhide + аудит 14/15, баннер-счётчик и диалог разбора на «Базах», «Добавить базу» =
  discovery-first, **ADR-29** + канон 03/04/05/06; live e2e на стенде (обе роли), бэкап БД
  `MitLicenseCenter_20260611_pre-mlc092.bak`; спека — план-файл
  `glittery-floating-turtle.md` (локальный, у куратора).
- **Трек «Обратный дрейф панель↔кластер»** — `MLC-095..097` 3/3 (2026-06-11), закрытие
  слепой зоны (б): `MissingItems[]` в ответе `/infobases/unassigned` (обратный diff из
  того же снапшота RAS, только при `Available:true`), красный баннер + метка `danger`
  «Не найдена в кластере» на строке + диалог с удалением существующим флоу; без таблиц/
  миграций/джобов/аудит-кодов; ADR-29 Update-нота + канон 04/05/06. **Пункт ROADMAP
  «Дрейф панель↔кластер» закрыт полностью** (оба направления, вместе с `MLC-092..094`);
  спека — часть 2 того же план-файла.
- **Трек «GUI-установщик (Inno Setup)»** — `MLC-098..104` (2026-06-11): бэкенд сам отдаёт SPA
  same-origin (**ADR-30**, IIS не нужен для хостинга панели) → self-contained single-file publish
  (`publish-release.ps1`) → GUI-установщик Inno Setup (`installer/MitLicenseCenter.iss` +
  `build-installer.ps1`, **ADR-31**): одна Windows-служба (service-aware `UseWindowsService`,
  `MLC-104`), мастер SQL/учётные данные (Windows-аккаунт или SQL-логин, тест подключения),
  оператор задаёт пароль admin, апгрейд поверх, деинсталл с keep-data prompt, ярлык «Пуск»;
  ADR-14 update-ноты (packaging ≠ CD). Спеки — план-файлы `installer-track.md` +
  `mlc-098..104`; полные отчёты — `PROJECT_BACKLOG_ARCHIVE.md`.
- **Вне трека (follow-up к «GUI-установщик»):** `MLC-105` Done (2026-06-11) — мастер
  распознаёт уже-инициализированную целевую БД (fail-open проба `auth.Users` тем же приёмом,
  что тест подключения; флаг `DbAlreadyInitialized` — один раз при уходе со страницы «Сеть»):
  предупреждает + пропускает страницу пароля admin (как на апгрейде), `.secret` не пишет;
  «чистая установка» ≠ «пустая БД». Канон — **ADR-31** + `OPERATIONS`. Только `.iss`;
  план `mlc-105-installer-existing-db.md`; отчёт — `PROJECT_BACKLOG_ARCHIVE.md`.
- **Вне трека (follow-up к «GUI-установщик»):** `MLC-106` Done (2026-06-11) — bootstrap создаёт БД
  на пустом инстансе. **Гоча:** EF `MigrateAsync` под `EnableRetryOnFailure` НЕ создаёт несуществующую
  БД (4060 ретраится как транзиентная → краш) — поэтому ранний сырой `CREATE DATABASE` к `master`
  (`DatabaseBootstrapper.EnsureDatabaseCreatedAsync`, `IF DB_ID IS NULL`, имя — параметром +
  экранирование `]`→`]]`) в `Program.cs` сразу после `builder.Build()`, ДО Hangfire-регистрации/Migrate/
  сидера; гейт — непустая `ConnectionStrings:Default` (InMemory-тесты пропускают). Канон — **ADR-18** +
  `OPERATIONS`; план `mlc-106-bootstrap-create-db.md`; отчёт — `PROJECT_BACKLOG_ARCHIVE.md`.
- **Вне трека (follow-up к «GUI-установщик»):** `MLC-107` Done (2026-06-11) — установщик: надёжное
  создание службы + чистый конфиг. Создание/конфиг/старт службы и firewall перенесены из `[Run]` в
  `[Code]` (`ConfigureService` в ssPostInstall) с проверкой `rc` `sc.exe`: на провале `sc create` —
  внятный `MsgBox` (особо **1057** — в Windows-режиме введён SQL-логин → подсказка выбрать
  SQL-аутентификацию) + `RaiseException` (откат; больше нет «успеха без службы»); `sc start` —
  предупреждение со ссылкой на Event Log. `appsettings.Production.json` сносится `[UninstallDelete]` и
  на **чистой** установке перезаписывается (skip-if-exists — только апгрейд, `ServiceExists`). Подписи
  режимов A/B — яснее. Только `.iss`. Канон — **ADR-31** + `OPERATIONS`; план
  `mlc-107-installer-service-robustness.md`; отчёт — `PROJECT_BACKLOG_ARCHIVE.md`.
- **Трек «Пред-релизные фиксы по итогам аудита»** — `MLC-108..115` 8/8 (2026-06-12..13):
  закрытие findings пред-релизного аудита А0–С1. Детерминированный состав артефакта (REL-01),
  немедленный отзыв доступа через SecurityStampValidator (SEC-01), NTFS-ACL секретов +
  честный ADR-8 «key ring plaintext» (KEYRING-01в/SEC-02/04), явный выбор клиента в форме
  привязки (UX-42), страховка апгрейда — экран бэкапа + провал старта службы = ошибка (REL-02),
  полное снятие IIS-публикации (`webinst -delete` за ADR-20, эндпоинт + UI + аудит
  `PublicationUnpublished=23`, UX-43), пакет валидации/наблюдаемости (BE-03/05/10 +
  аудит `LoginFailed=108` + UX-01-остаток), `THIRD_PARTY_LICENSES.txt` в поставку + тесты
  profile/ForcePasswordChange (REL-10 + FE-01). Спека — `audit-fix-prerelease.md`;
  индекс задач — `PROJECT_BACKLOG_ARCHIVE.md`; вердикт/findings — `audit/2026-06/MASTER-REPORT.md`.
  **После трека (вне задач, ведёт куратор):** ручной прогон Setup.exe на чистой ВМ (слепая зона
  аудита) → этап D1 «Документация v2» (промт `audit-prerelease-prompts.md`;
  выровнять security-слой и 02, терминологию DPAPI→plaintext в ADR-21/CLAUDE.md) → перенос
  roadmap R1–R12 из `TECH-DEBT.md` в `ROADMAP.md`.
- **Вне трека (follow-up ВМ-проверки к установщику):** `MLC-116` Done (2026-06-13) — апгрейд:
  служба реально останавливается перед заменой файлов; ложная ошибка 1056 устранена.
  **Первопричина:** `StopServiceAndWait` передавал в `ControlService` access right
  `SERVICE_STOP=$0020` вместо control-кода `SERVICE_CONTROL_STOP=$0001` → `ERROR_INVALID_PARAMETER`,
  служба не останавливалась → restart-manager «файлы заняты» + после RM-перезапуска `sc start`=1056.
  Фикс: верный control-код; `sc start` rc=1056 (ALREADY_RUNNING)=успех (не предупреждение, не
  `ServiceStartFailed`); тексты «не запустилась» вместо ложного «создана» на апгрейде; чистая
  установка (create/1057/RaiseException/ACL/.secret) не тронута. Только `.iss`; ISCC-компиляция
  `[Code]` зелёная, живой апгрейд-прогон на ВМ — за владельцем. Отчёт — PR #138 / коммиты `MLC-116:`.
- **Вне трека (follow-up ВМ-проверки к установщику):** `MLC-117` Done (2026-06-13) — публикация видит
  `OneC.RAS.Endpoint` сразу после установки (раньше падала «Не задан адрес 1С-кластера» до пересохранения).
  **Первопричина:** у ключа не было сидового дефолта → на свежей установке `ValueText=null`, а FE
  (`RasPortField`/`parseRasPort`) рисовал фантомный «1545» при неактивной «Сохранить» → значение
  не персистилось; discovery работал (rac.exe молча → localhost при пустом endpoint),
  публикация падала (`WebinstArgs.ResolveClusterServer` бросает на пустом). Оба пути делят
  один TTL-снапшот — расхождения чтения нет. **Фикс:** `DefaultValue: "localhost:1545"` в
  `SettingDefinitions` (свежая БД) + `SettingsSeeder.HealRasEndpointAsync` — одноразовый heal
  существующей пустой строки на апгрейде (дефолт из каталога, только при пустом `ValueText`+null
  `Value`, идемпотентно, чужое не трогает). `ResolveClusterServer` не менялся (ошибка остаётся для
  реально пустого). Без миграций/FE/parity. Отчёт — PR #140 / коммиты `MLC-117:`.
- **Трек «Пострелизный рефакторинг» R1–R12** — `MLC-118..139` (2026-06-13..14): закрытие всего
  остаточного техдолга аудита (`audit/2026-06/TECH-DEBT.md`) по итерациям `ROADMAP.md` — валидация,
  аудит-целостность, тест-сети, офлайн-UX, видимость лимитов, устойчивость джобов, релизный конвейер,
  security-барьер (ADR-41..44), масштабируемость списков (R11), полировка UX + optimistic concurrency
  (R12). Индекс задач — `PROJECT_BACKLOG_ARCHIVE.md`; статусы итераций — `ROADMAP.md`.
- **Трек «Релиз 0.4.0-beta»** — `MLC-140..144` (2026-06-14): первый пострелизный фичевый цикл —
  Dependabot-обновления (140), упрощение диалогов подтверждения (141, ADR-45), полировка
  «Быстродействия» (142), графики дашборда на recharts (143), движок таблиц `@tanstack/react-table`
  (`DataTable`, 144a..e, **ADR-46**: 6 списков управления; perf-виджеты на shadcn Table). Версия
  забампана `0.4.0-beta`. Индекс задач — `PROJECT_BACKLOG_ARCHIVE.md`; статусы — `ROADMAP.md`.
- **Трек «Релиз 0.5.0-beta»** — `MLC-147..152` + `MLC-027` (2026-06-14): версия в сайдбаре (149),
  фикс холодного старта `/sessions` (148), xlsx-CDN security (147), разбиение i18n (027), серверный
  фильтр «не найдена в кластере» (150), optimistic concurrency Infobase/Publication (151); SQL
  least-privilege (152) **проведён трассой, но откачен** `MLC-153` — для мультитенанта с динамическими
  базами `sysadmin` штатен, бэкап-rewrite не давал выгоды (вывод трассы — в ADR-44).
  **Strategy B отклонена** (ADR-3.3). Версия забампана `0.5.0-beta`. Индекс — `PROJECT_BACKLOG_ARCHIVE.md`.
- **Трек «Свежесть данных на страницах»** — `MLC-154..156` 3/3 (2026-06-15): устранение задержки
  появления данных. Cold-цикл сеансов вынесен из Hangfire в `ColdTierPollingService : BackgroundService`
  — настройка `Polling.ColdIntervalSeconds` стала реальной (60с→дефолт 15с), ADR-6.1 Update;
  `refetchOnWindowFocus: true` (155); `LiveControls` Пауза/«Обновить сейчас» на /sessions (live-форс
  через `ISessionRefreshTrigger` + `POST /sessions/refresh`) и «Быстродействии» (156). Спека —
  `parsed-mixing-puppy.md`; индекс — `PROJECT_BACKLOG_ARCHIVE.md`.
- **Трек «Авто-регистрация службы RAS»** — `MLC-159..161` 3/3 (2026-06-15, релиз `0.5.4-beta`,
  **ADR-47**): панель сама управляет локальной службой RAS через `sc.exe` — обнаружение по
  `binPath` с `ras.exe`, диагностика из 4 состояний (нет / устарела под другую платформу /
  остановлена / ок), регистрация / перерегистрация под выбранную платформу+порт / запуск
  (порт `IRasServiceManager`, аудит 600-серии); блок лечения в Настройках (Admin, диалог с
  точной командой `sc …`, ленивый `/status`); сигнал недоступности на дашборде (дешёвый
  health, не `/status`). Version-agnostic (8.3/8.5). Спека — `ras-service-autoregister.md`;
  индекс — `PROJECT_BACKLOG_ARCHIVE.md`. **Живой прогон на стенде — за владельцем.**
- **Трек «Релиз `0.6.0-beta`»** — `MLC-164..166` 3/3 (2026-06-15, **ADR-48**): достоверность лицензий +
  читаемость. `ConsumesLicense` = факт `rac --licenses` (не эвристика app-id), холодный тир — второй
  вызов `--licenses` + сшивка по `session`-GUID, горячий — кэш `ILicenseFactCache`, трёхсостояние
  `LicenseStatus` + пауза enforcement при сбое rac + grace-guard (166); аудит при добавлении базы —
  одна запись `InfobaseCreated` (164); «Сеансы» — фильтр по типам сеансов, по умолчанию пользовательские
  (165). Версия `0.6.0-beta`. Спайк rac — на стенде; индекс — `PROJECT_BACKLOG_ARCHIVE.md`.
  **Живая проверка на стенде — за владельцем.**
- **Вне трека (follow-up к `0.6.0`):** `MLC-167` (FE, релиз `0.6.1-beta`) — «Сеансы»: тумблер «Только
  лицензионные» **ВКЛ по умолчанию** (полный список фактически потребляющих лицензию, перекрывает
  типы); фильтр типов — полный каталог `KNOWN_APP_IDS` ∪ незнакомые присутствующие + «Выбрать все»;
  `SearchableMultiSelect` select-all, новый `Switch`-примитив. Индекс — `PROJECT_BACKLOG_ARCHIVE.md`.
- **Вне трека (follow-up к `0.6.0`):** `MLC-168` (BE, релиз `0.6.2-beta`) — отчёт «Использование
  лицензий»: холодный цикл не сэмплирует при недоступном факте rac (`licenseFactAvailable=false`) —
  честный пробел в истории вместо ложного 0 (все сеансы `Pending` дали бы `consumed=0`). Тест на
  пропуск сэмпла. Канон — ADR-48. Индекс — `PROJECT_BACKLOG_ARCHIVE.md`.
- **Вне трека (housekeeping, релиз `0.6.3-beta`):** `MLC-169` (BE) — ретеншен записей «Быстродействия»:
  ночная джоба `perf-recording-retention` (03:45 UTC) чистит `PerfRecordings` + сэмплы старше **90 дней**
  (срок зашит константой, не Setting — прецедент `JobRetentionStateFilter`; ноль новых настроек в панели).
  Двухфазное построчно-батчевое удаление (дети→родители, без опоры на каскад), provider-portable, под
  execution strategy (MLC-074); активную запись не трогает. Закрыта единственная асимметрия retention
  (у `PerfRecordings` не было очистки). Канон — таблицы джоб 02/04 + OPERATIONS. Индекс — `PROJECT_BACKLOG_ARCHIVE.md`.
- **Вне трека (Security / Installer):** `MLC-170` (**ADR-49**) — служба под **virtual service account**
  `NT SERVICE\MitLicenseCenter` (по умолчанию) / именная Windows-учётка / gMSA; `Trusted_Connection` к
  локальному SQL → **SQL-пароль убран из `appsettings.Production.json`** (режим B/LocalSystem удалён).
  Установщик сам создаёт SQL-логин учётки (`CREATE LOGIN … FROM WINDOWS` + `sysadmin`, идемпотентно,
  hard-fail; предусловие — установщик-админ = `sysadmin`) и добавляет учётку в локальных Администраторов
  (best-effort). Рефактор порядка `ssPostInstall` (`RegisterService`→`ProvisionSqlLogin`→ACL→
  `StartServiceAndFinalize`, `sc start` последним). Только `.iss` + present-tense `SECURITY`/`INSTALL`/
  `OPERATIONS` + ADR-31/40/44 к факту; бэкенд не тронут. ISCC-компиляция зелёная. **Живая проверка на
  стенде — за владельцем.** Миграция B→A — `OPERATIONS.md §8`. Спека — `gleaming-sparking-cat.md`.
- **Вне трека (Security / Installer, follow-up к ADR-49):** `MLC-171` — провижининг-креды **на выбор**:
  (дефолт) Integrated Security запускающего админа / (запасной) введённый SQL-логин с ролью `sysadmin`
  (`sa`/иной, для mixed-mode). Снимает жёсткое предусловие MLC-170 «запускающий админ обязан быть
  sysadmin». **Создаётся всегда Windows-логин** (`FROM WINDOWS`); SQL-логин транзиентен — в конфиг не
  пишется, нигде не сохраняется (инвариант «секрета SQL на диске нет» цел). Новая страница мастера
  «Подключение установщика к SQL» (radio + логин/пароль + перенесённый тест); `ProvisioningConnString`
  ветвит `TestSqlConnection`/`DatabaseHasPanelUsers`/`ProvisionSqlLogin`. Только `.iss` + present-tense
  `SECURITY`/`INSTALL`/`OPERATIONS`; ADR-49-нота уже влита. ISCC зелёный. **Живая проверка — за
  владельцем.** Спека — `gleaming-sparking-cat.md` (раздел MLC-171).
- **Вне трека (Installer, фикс по стенду):** `MLC-172` — на живом стенде выявлены: (1) `ProvisionSqlLogin`
  падал «Неправильный синтаксис около "+"» — `EXEC sp_executesql N''..''+QUOTENAME(..)` недопустим
  (конкатенация в аргументе proc); фикс — собирать в `@sql` и `EXEC sys.sp_executesql @sql` (проверено
  на живом SQL); (2) текст мастера слишком многословен — подписи страниц/радио/полей сжаты, детали в
  доках. Также код 1072 (`ERROR_SERVICE_MARKED_FOR_DELETE`) и 1057 — это состояние стенда/ввод, не баг.
  **Урок:** установщик-T-SQL гнать против живого SQL до merge — ISCC-компиляция семантику не ловит.
  (в) на стенде 0.6.5: поля SQL-логина не отображались — кастомные контролы вставали ниже растянутого
  `CheckListBox` radio-страницы; страница переведена на родную `InputQueryPage` (поля гарантированно
  видны; режим из поля логина: пусто=Integrated). **Урок:** кастомные input-контролы не класть на
  `CreateInputOptionPage`. Только `.iss` + бамп. Релизы `0.6.5`→`0.6.6-beta`. **Стенд — за владельцем.**
- **Трек «Рефакторинг + редизайн установщика» — этап 1 (Трек 1):** `MLC-173` Done (2026-06-16, PR #280) —
  монолитная секция `[Code]` (~1600 строк) `installer/MitLicenseCenter.iss` разнесена по 8 модулям
  `installer/include/*.iss` (`code-declarations`/`input-accessors`/`service-control`/`strings-connstr`/
  `config-acl`/`service-sql`/`sql-test`/`wizard`), подключаемым `#include` в текущем порядке; главный
  `.iss` — директивы (1–150) + карта модулей. **Поведение байт-в-байт** (конкатенация include == строки
  151–1748 побайтно, машинно доказано; добавлен лишь инертный `{ }`-комментарий-карта). Include — UTF-8
  c BOM, LF. ISCC зелёный, `build-installer.ps1`/CI не тронуты, версия не бампнута. Канон — `INSTALL` §3 +
  `DEVELOPMENT`. Спека — `atomic-coalescing-wave.md`. **Этап 2 (Трек 2, редизайн GUI) — следующий, за куратором.**
- **Чистка кода установщика (перед Треком 2):** `MLC-174` Done (2026-06-16) — поведение-сохраняющий
  рефактор по аудиту лучших практик: единый помощник `RunPowerShellFile` (4 SQL/provisioning-шага
  делили один паттерн запуска временного `.ps1`; путь к `powershell.exe` теперь в одном месте),
  общий хвост `ScObjectTail` для `sc create/config`, сняты висячий параметр `Param` у
  `GetScCreateParams/GetScConfigParams/GetFirewallAddParams` (`PanelUrl` не тронут — он реально
  `{code:}`) и мёртвая константа `SERVICE_STOP_PENDING`. Строки `sc` и тела `.ps1` идентичны —
  поведение не меняется; ISCC зелёный, версия не бампнута. **Tier A трогает SQL-путь → дымовой прогон
  на стенде за владельцем (вместе со стендом Трека 2).** Опция на потом: `ExecAndCaptureOutput`
  (Inno 6.3+) вместо временного out-файла в `DatabaseHasPanelUsers` — отложено (косметика на проверенном пути).
- **Редизайн GUI мастера (Трек 2):** `MLC-175` Done (2026-06-16, релиз `0.7.0-beta`) — выбор личности
  провижининга стал **явным**: радио-страница «Подключение установщика к SQL» (Integrated Security /
  SQL-логин) + условная страница «Учётные данные SQL-логина» (только при SQL-логине, `ShouldSkipPage`) —
  вместо неявного «пусто = Integrated» (MLC-172); `ProvisioningMode` читает `SelectedValueIndex`. Кнопка
  «Проверить подключение» убрана — авто-тест при «Далее» (Integrated: со страницы режима; SQL-логин: со
  страницы кредов; сброс `ConnTestPassed` при входе на обе). Экран «Готово» — сводка через `UpdateReadyMemo`.
  Родные контролы Inno (radio+edit на одной странице не рисуются — баг MLC-172). Инварианты не тронуты:
  порядок `ssPostInstall` (ADR-49), провижининг `FROM WINDOWS`+sysadmin, gMSA-чекбокс, ACL. ISCC зелёный.
  Канон — `INSTALL` §3.4/§3.9, ADR-31/49 update-ноты, `SECURITY`. Спека — `binary-sniffing-pinwheel.md`.
  **Живой стенд за владельцем.**

---

## Открытые опции (deferred — не активные задачи)

Объём = новая работа; брать по появлению **триггера**, не по умолчанию. Триаж 2026-06-10:
снятые опции и обоснование — в архиве («Триаж отложенных опций»).

- `MLC-025` — **OpenAPI-codegen / расширение Zod-границ** (поглощает `MLC-006(a)`). Сейчас типы
  рукописные, Zod на 3 критичных границах (ADR-10.1, MLC-016). Триггер: рост API-поверхности /
  частые расхождения FE↔BE. Даёт инфраструктуру для `MLC-026`. Объём M.
- `MLC-026` — **генерация FE-полей настроек из `SettingDefinitions`**. Сейчас новый ключ касается
  enum + каталога + захардкоженного FE-рендера (`SettingsPage.tsx` `SECTIONS`/`FIELD_META`) + i18n
  (подтверждено на MLC-024). Зависит от `MLC-025`. Триггер: каталог > ~25–30 ключей (сейчас 17).
  Объём S.
- `MLC-145` — **миграция с FluentAssertions на свободный форк** (зафиксировано на MLC-140).
  Тесты используют FA 6.12.2 (120 файлов, ~1660 `.Should()`); ветка зафиксирована — **FA 8.x под
  коммерческой лицензией Xceed**, поднимать нельзя, а 6.x не получает патчей. Дешёвый путь —
  drop-in-форк `AwesomeAssertions` (FA-7, Apache-2.0; тот же namespace/API → правок ассертов
  почти нет). Заодно: добавить `ignore` мажора FluentAssertions в `.github/dependabot.yml`, чтобы
  не плодить еженедельные PR на FA 8. Триггер: security-issue в FA 6.x **или** шум от Dependabot.
  Объём S.

> `MLC-073` (полировка «Быстродействия») и `MLC-062` (движок таблиц) закрыты в релизе
> `0.4.0-beta` как `MLC-142` и `MLC-144`.

---

## Закрыто

`MLC-001..175` закрыты (кроме отложенных `MLC-025/026/145` — см. «Открытые опции»; `MLC-062`/`073`
закрыты в релизе `0.4.0-beta` как `MLC-144`/`142`; `MLC-080` закрыта в составе `MLC-081`,
`MLC-079` — в составе `MLC-084`; `MLC-011(a)`, `MLC-028`, `MLC-006(a)` сняты с реестра триажем
2026-06-10 — их несут ADR-20 / DECISIONS «Deployment topology» + ROADMAP / запись `MLC-025`;
`MLC-036` — ADR-3.3 «RAS Strategy B», отклонена). Трек релиза `0.4.0-beta` (`MLC-140..144`), трек
релиза `0.5.0-beta` (`MLC-147..152` + `MLC-027`), трек «Свежесть данных на страницах»
(`MLC-154..156`), трек «Авто-регистрация службы RAS» (`MLC-159..161`, релиз `0.5.4-beta`, ADR-47)
патч `0.5.5-beta` (`MLC-162` — фикс обнаружения службы RAS через реестр) и патч `0.5.6-beta`
(`MLC-163` — фикс «Проверить публикацию», concurrency-токен в джобе статусов), трек релиза
`0.6.0-beta` (`MLC-164..166`, ADR-48 — лицензии = факт `rac --licenses`), follow-up `0.6.1-beta`
(`MLC-167` — «Сеансы» по умолчанию показывают лицензионные) и фикс `0.6.2-beta` (`MLC-168` — отчёт не
сэмплирует ложный 0 при сбое rac) **завершены** — индексы в `PROJECT_BACKLOG_ARCHIVE.md`.
`MLC-146` — уборка мёртвого кода после MLC-144. Полные отчёты — в git-истории (коммиты `MLC-NNN:`/PR).
**Нет активного трека.**

**Следующий свободный номер: `MLC-188`.** (`MLC-177..184` — этап 1 релиза 1.1.0 (вкл. stand-фидбэк
182/183/184); `MLC-185..187` выданы этапу 2 — размер баз · редизайн Обзора · ESLint.)
Куратор инкрементирует при выдаче; выдавать занятые номера нельзя (прецедент коллизии
`MLC-053`/`054`).
