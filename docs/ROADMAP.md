# MitLicense Center — Roadmap

Документ описывает **планы развития вперёд** и отложенный бэклог — то, что ещё не сделано.
Горизонт — пострелизный цикл (v1.x и далее). История выпусков здесь не ведётся:
закрытые задачи — индекс в [`PROJECT_BACKLOG_ARCHIVE.md`](PROJECT_BACKLOG_ARCHIVE.md) (поиск по
`MLC-NNN`), развёрнутые отчёты — в git-истории. Продуктовый контекст — [`01_OVERVIEW.md`](01_OVERVIEW.md);
архитектурные ограничения — [`DECISIONS.md`](DECISIONS.md).

---

## Актуальные планы

Расширенный фичебатч **1.1.0** (мониторинг размера баз MSSQL, живой редизайн «Обзора»,
правило-линтер `StatusBadge` + улучшения отчётов, бэкапов и управления списками) **выпущен в
`v1.1.0`** (2026-06-17) — история в [`PROJECT_BACKLOG_ARCHIVE.md`](PROJECT_BACKLOG_ARCHIVE.md)
(поиск по `MLC-177..189`) и git.

Минор **1.1.1** (причина завершения сеанса + контакты провайдера, ночные джобы по местному поясу
хоста, единый поиск в «Аудите», точные статусы квоты на «Обзоре», агентский порт 1С → настройка)
**выпущен в `v1.1.1`** (2026-06-18) — `MLC-190..194`, история в
[`PROJECT_BACKLOG_ARCHIVE.md`](PROJECT_BACKLOG_ARCHIVE.md) и git.

**Редизайн UX/UI** (строгий монохром + тёмная тема по умолчанию, «Сеансы» = дом темы лицензий,
«Отчёты» растворены → меню 8→7, «Быстродействие» = воронка расследования) **выпущен в `v1.1.2`**
(2026-06-18) — `MLC-195..210`, **ADR-53**; история в [`PROJECT_BACKLOG_ARCHIVE.md`](PROJECT_BACKLOG_ARCHIVE.md)
и git, спека — локальный план-файл `.claude/plans/redesign-ux.md`.

**Трек «Сервер»** (раздел меню «Сервер» 7→8: управление службами узла — сервер 1С / RAS / IIS,
рабочие процессы `rphost` со списком и рестартом, расписание авто-рестартов; мониторинг обслуживания
SQL — свежесть бэкапов и планы обслуживания с детализацией по задачам; SQL только наблюдение)
**выпущен в `v1.1.3`** (2026-06-19) — `MLC-212..221`, **ADR-54/55/56**; история в
[`PROJECT_BACKLOG_ARCHIVE.md`](PROJECT_BACKLOG_ARCHIVE.md) и git.

**Устойчивость службы** (отзыв жёсткой SCM-зависимости службы `MitLicenseCenter` от SQL — ручная
остановка SQL больше не гасит панель; панель сама переподключается, устойчивость — на recovery-политике)
**выпущена в `v1.1.4`** (2026-06-19) — `MLC-227`, пересмотр **ADR-40**; история в
[`PROJECT_BACKLOG_ARCHIVE.md`](PROJECT_BACKLOG_ARCHIVE.md) и git.

---

**Роадмап-план v1.1.x выпущен полностью — активного трека нет.** Новые планы вперёд ставит куратор
постановочным PR. Прочие кандидаты — отложенные опции в [`PROJECT_BACKLOG.md`](PROJECT_BACKLOG.md)
(раздел «Открытые опции»).

---

## Отложенные доковые правки (на будущее)

_Сейчас пусто — отложенных доковых правок нет._

> **Принятые «нет/потом» здесь не дублируются** — это решения, а не планы вперёд, их дом —
> [`DECISIONS.md`](DECISIONS.md): multi-cluster/multi-node gated за **ADR-28** (single-host);
> RAS Strategy B отклонён (**ADR-3.3**); планировщик резервного копирования, restore-оркестрация
> и встроенная 2FA — вне сферы навсегда (**ADR-15**; единственное исключение — ручной on-demand
> `COPY_ONLY`-бэкап, **ADR-27**).
