// Пустышка для НЕиспользуемых опциональных зависимостей jsPDF (MLC-052).
//
// jsPDF динамически (`import()`) тянет html2canvas / canvg / dompurify только в методах
// `.html()` и SVG-рендера — мы их НЕ вызываем (график рисуем сами через Chart.js →
// PNG → addImage, текст и таблицу — нативно). Без подмены эти тяжёлые либы (html2canvas
// ≈ 200 кБ + canvg + dompurify) затягиваются сборщиком в общий eager-`vendor`, раздувая
// основной бандл. Алиасим их на этот пустой модуль (vite.config) — в бандл не попадают.
// Если когда-нибудь понадобится jsPDF.html()/SVG — убрать алиасы и вернуть зависимости.
export default {};
